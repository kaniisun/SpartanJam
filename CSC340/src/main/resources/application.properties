## Connects Website to XAMPP Apache and MYSQL Database
spring.application.name= CrudAssignment
spring.datasource.url = jdbc:mysql://localhost:3306/spartan-db?useSSL=false
spring.datasource.username = root
spring.datasource.password = 

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type=TRACE
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

#logging.level.org.springframework.jdbc.core.JdbcTemplate=DEBUG
#logging.level.org.springframework.jdbc.core.StatementCreatorUtils=TRACE

# Hibernate ddl auto (create, create-drop, validate, update)

## Ensures that information gets updated but also takes the audio uploaded to store into the audio folder 
spring.jpa.hibernate.ddl-auto = update
spring.resources.static-locations= classpath:/static/
upload.path=src/main/resources/static/audio/

# Maximum file size for uploads (1GB in this example)
spring.servlet.multipart.max-file-size=1GB
c/